<!DOCTYPE html>
<html>
<head>
   <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tag %>
  <link href="https://fonts.googleapis.com/css?family=Poppins" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">AirBnB Clone</a>
      </div>
      <ul class="nav navbar-nav">

        <% if signed_in? -%>
          <% if current_user.customer? -%>
            <li class="home"><%= link_to t("Home"), sign_in_path %></li>
            <li class="all_listings"><%= link_to t("All Listings"), listings_all_path %></li>
            <li id="sign_out">
              <li class="your_listings"><%= link_to t("Your Listings"), user_listings_path(current_user.id) %></li>
                <%= button_to t(".sign_out"), sign_out_path, method: :delete %>


            <% elsif current_user.moderator? -%>

              <li class="home"><%= link_to t("Home"), sign_in_path %></li>
              <li class="all_listings"><%= link_to t("All Listings"), listings_all_path %></li>
              <li id="sign_out">
                <li class="unverified_listings"><%= link_to t("Verify Listings"), user_listings_path(current_user.id) %></li>
                  <%= button_to t(".sign_out"), sign_out_path, method: :delete %>
              <% end -%>

              <% else -%>
                <%= link_to t(".sign_in"), sign_in_path %>
            <% end -%></li>
      </ul>
    </div>
  </nav>

  <div id="flash">
    <% flash.each do |key, value| -%>
      <div id="flash_<%= key %>"><%=h value %></div>
    <% end %>
  </div>

  <%= yield %>
</body>
</html>
